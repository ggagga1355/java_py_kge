**참조변수, 객체

-----------------------------------------------------------------------------------------------------------------------------
클래스도 타입이 될 수 있다.

static으로 정의하면 method에 하나만 저장한다.

메서드 구조 숙지
접근제한자 반환타입 메서드명(매개변수){
	반환할 값이 있다면 return 반환값
	없다면 void
}























